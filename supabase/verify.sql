-- Run this in Supabase SQL Editor
-- If this gives an error, the table DOES NOT exist.
-- If this returns a row (or empty result), the table EXISTS.
SELECT * FROM public.communication_questions LIMIT 1;
